# مراجعة المطابقات القريبة لصور Grade 5

## معيار الاعتماد الجديد

تنفيذًا لطلب المعلم، تُقبل الصورة إذا قدّمت تمثيلًا مباشرًا أو قريبًا مفيدًا للمفردة في سياق تعليمي للأطفال. تُستبعد فقط الصورة البعيدة عن المعنى أو التي يتطلب ربطها تفسيرًا غير ظاهر.

## مراجعة اللوحتين 1–2

- تُقبل الأمثلة المباشرة أو القريبة الواضحة مثل: `designed`، `stare`، `rested`، `swim`، `turn over`، `even in the desert`، و`searched`.
- يبقى ربط كرة تصطدم بالحائط بمفردة `blocked by` غير دقيق؛ لا يعتمد هذا الزوج ما لم تتوفر مفردة `hit` غير مرتبطة أصلًا.
- الصور الواقعية أو الرسومية ذات الشروح الكثيفة قد تُستخدم فقط عندما تمثل المفردة بوضوح ولا توجد صورة كرتونية أنسب، مع رفض أي مادة مائية أو نص مزدحم يمنع الاستعمال التعليمي.
- الصور البعيدة أو العامة جدًا تبقى مستبعدة حتى مع توسيع معيار القبول.

## مراجعة اللوحتين 3–4

- تُقبل الصور الواضحة للمفردات: `contain`، `shone`، `added`، `grilled`، `cut ... into`، `replaced`، `give advice`، `thunderstorm`، `thin slices`، `wildfire`، `decide`، و`received`.
- تقبل الصور الفوتوغرافية أو المخططات المباشرة للمعنى عند عدم وجود بديل كرتوني قريب، استجابة لطلب المعلم بتوسيع الاستفادة من الصور المتاحة؛ لا تُقبل الصور ذات العلامات المائية أو النصوص المزدحمة التي تمنع الفهم.
- تظل صور `waiter` ذات الخلفية الشبكية وصور الأمثلة التي تحمل نصوصًا تعليمية ثقيلة مرشحة للاعتماد فقط إن لم يوجد بديل أنسب؛ يعاد فحصها عند دمج الاختيارات النهائية.

## مراجعة اللوحتين 5–6

- تُقبل الصور المباشرة أو القريبة المفيدة مثل: `turn right side up`، `skills`، `ingredients`، `Khamsin`، `hide`، `realized`، `life saver`، `pumped`، `pump`، `modern technology`، `mud bricks`، `recipe`، `damaged`، و`taking care of`.
- تستعمل إنفوغرافات `tips` و`recipe` و`turn right side up` لأن النص جزء من الشرح التعليمي وليس علامة مائية، كما أن معناها ظاهر للطفل.
- تُستخدم الصور الواقعية ذات العلاقة المباشرة بالمعنى، مثل إنقاذ السباح أو مضخة الماء أو الطوب الطيني، وفق معيار القرب التربوي الذي طلبه المعلم.

## مراجعة اللوحتين 7–8

- تُقبل الصور الخاصة بـ `stay in balance` و`theme park` و`encouraged` و`heat wave` و`crunchy / crispy` و`warnings` و`irrigation` و`drought` و`responded`.
- تُقبل المخططات الواضحة التي تشرح مفردات العلوم مثل `increase` و`blood vessels` و`organ` و`blood`؛ فالنص جزء من المادة التعليمية ولا توجد علامة مائية ظاهرة.
- تُقبل المطابقات المباشرة مثل `then and now` و`bake` و`changed` و`iron`، مع استمرار تجنب أي رسم توضيحي لا يشرح المفردة مباشرة.

## مراجعة اللوحتين 9–10

- تُقبل المطابقات القريبة المفيدة مثل `prevent` و`personally` و`take millions of years` و`treatment` و`managed` و`stay healthy` و`make our bodies stronger` و`giving back`.
- تُقبل صور `wonders` و`real-life heroes` و`mental health` و`journey` و`come back home` و`work around the clock` لكونها تقدم سياقًا قريبًا ومفهومًا للمفردة.
- تظل صورة النحلة مع الشرح المطوّل مرشحة لمفردة `play an important role` بسبب وضوح المثال، ويمكن استخدامها مع مراعاة أن النص التعليمي جزء من الصورة وليس علامة مائية.

## مراجعة اللوحتين 11–12

- تُقبل الصور القريبة لـ `kings of the Nile` و`over their fields` و`ready to help` و`diagnosed` و`began` و`lost` و`deep` و`seem` و`diseases`.
- تُقبل أيضًا الصور التي تشرح `gas` و`met` و`flavor` و`respond` و`keep ... from` و`What a pity!` و`come with` و`knowledge` و`fertile`.
- لا تظهر علامات مائية مرئية في هذه اللوحات. تُقبل النصوص الوظيفية القصيرة داخل الرسوم، مثل `MET!` أو `FLAVOR`، لأنها تعزز المفردة ولا تمثل علامة ملكية.

## مراجعة اللوحة 13

- تُقبل صور `severe/extreme` و`fried` و`harmful`؛ كلها تمثل المعنى بوضوح ولا تحمل علامة مائية ظاهرة.
- اكتملت مراجعة اللوحات الثلاث عشرة. ستُستبعد فقط المرشحات التي وُسِمت صراحةً بأنها بعيدة أو غير مناسبة بصريًا في هذه الملاحظات، وتُعتمد بقية المطابقات المباشرة والقريبة وفق معيار المعلم.
