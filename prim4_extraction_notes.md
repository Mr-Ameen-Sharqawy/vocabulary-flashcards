# ملاحظات استخراج منهج الصف الرابع

## مصدر الملفات

أرشيف المستخدم: `/home/ubuntu/upload/Prim4_fixed(1).zip`، بعد استخراجه في `/home/ubuntu/prim4-source/Prim 4/`.

## الفهرس الأولي

يضم الأرشيف 20 ملف HTML: وحدات 1 إلى 5 مع دروس متاحة، وملف قصة للوحدة 6. أسماء الملفات تحمل ترميز الوحدة والدرس، مثل `Prim 4 U 1 L 1.html` و`Prim 4 U 1 L 4&5.html`. لا يظهر ملف مستقل للوحدة 4، الدرس 3 ضمن الفهرس.

## تحقق من البنية: Unit 1 · Lesson 1

المصدر: `Prim 4 U 1 L 1.html`. يحمل العنوان **Unit One: Our Amazing Senses** و**Lesson 1: What are the Five Senses?**. البيانات منظمة داخل مصفوفات JavaScript ثابتة: `mainVocab` و`extraVocab` و`expressions` و`verbs` و`keySentences`؛ لذا يمكن استخراجها نصيًا دون تشغيل الصفحة.

المفردات الأساسية المتحققة: `taste`, `touch`, `hearing`, `sight`, `smell`, `fire`, `smoke`, `voices`, `tongue`.

## منهجية الاستخراج

تُقرأ الملفات كنص فقط. تُستخرج المصفوفات محددة الاسم باستخدام أنماط نصية ثابتة، ثم يُنشأ ملف JSON ملخص. لا تُشغّل أي شيفرة من ملفات الأرشيف.

## النتيجة المتحققة

استُخرجت 20 حزمة درس، تضم 497 بطاقة مفردات وتصريفات قبل إزالة التكرار، و469 مفردة إنجليزية مميزة. تضمن البيانات عنوان الوحدة والدرس، والإنجليزية والعربية ونوع المفردة وجملة إكمال مناسبة لكل بطاقة.

احتاج قالبان فقط إلى تنقيح يدوي لأنهما يعرضان متغيرًا نحويًا بدل كلمة مكتملة: `It's fun to + inf.` و`proud to + inf.`. حفظت النسخة المنظمة في `prim4_course_cards.json`، مع الاحتفاظ بالنص الأصلي في حقل `sourceTerm` للرجوع إليه.

## قرار الأصول البصرية

لأن 469 مفردة مختلفة تتجاوز نطاق الصور البصرية المناسبة دفعة واحدة، ستستخدم البطاقات أولًا صورًا تعليمية مولدة على مستوى **موضوع الدرس** مع رموز بصرية مرتبطة بالكلمة. تُخصَّص صور مفردة للكلمات المصورة بوضوح أو الأكثر مركزية داخل كل درس، مع المحافظة على ترابط بصري واحد للمنهج كله.
