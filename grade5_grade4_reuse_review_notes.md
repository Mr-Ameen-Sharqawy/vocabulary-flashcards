# Grade 4 → Grade 5 image-reuse visual review

## Sheets 1–2 reviewed

The following high-confidence semantic candidates visibly illustrate an equivalent or directly supportive Grade 5 meaning and may be accepted after the full review: `regularly` ← `daily`, `exercising` / `exercised` ← `do exercises`, `healthier` ← `healthy`, `powerful` ← `strong`, `stay active` ← `do exercises`, `provide` / `provided` ← `offer` / `offered`, `close to` ← `near`, `movement` ← `move`, `community` ← `neighborhood`, `peaceful` ← `quiet`, `the busiest place` ← `busy market`, `shop` ← `stores`, `gather` ← `collect`, `fed` ← `feed`, `form` / `formed` ← `create` / `created`, `cost` ← `price`, and `bathe` ← `take a shower`.

The following candidates are not sufficiently precise in the visible artwork and should not be reused automatically: `realized` ← `noticed`, `all day` ← `during the day`, `alive` ← `lives`, `knowledge` ← `know`, and `cover` ← `covered with`.

The visual for `kindness` / `with a little bit of kindness` was not present in the local Grade 4 cache but its curated Grade 4 source label is `acts of kindness`; it requires a final source-asset check before reuse.

The following terms can be considered only if their Grade 5 sentence context confirms the intended sense: `live` / `lived` ← `lives`, `tasty` ← `yummy`, `normal` ← `usual`, `admire` / `admired` ← `impressed by`.
