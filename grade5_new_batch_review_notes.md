# Grade 5 new-batch visual review notes

## Sheets 1–2

### Suitable or promising for the intended term

`roads`, `rides`, `relaxing`, `affected` (plant visibly affected by heat), `sandstorm`, `coffee shop`, `miners`, `supermarket`, `fist`, `steel`, `basin` (choose the cartoon basin over the Egyptian scene where style consistency matters), `stressed`, `thick` (choose the cartoon thick book over the photograph), and `toothpaste` are visually clear and match their proposed meanings.

### Do not accept automatically

`deep`, `side`, `flood`, and `stethoscope` are diagrammatic or text-heavy rather than consistent cartoon vocabulary art. `waiter` has a visible checkerboard-like background and should be rejected. `grilled`, `thick` (photograph), and `thunderstorm` are photographs and should not displace a suitable cartoon option. The accepted `basin` and `thick` choices above avoid their weaker alternatives.

### Follow-up

Sheets 3–4 still require visual review. All accepted candidates remain provisional until the full batch is reconciled with the existing Grade 5 map and no duplicate meaning or image is introduced.

## Sheets 3–4

### Suitable or promising for the intended term

`modern` (modern solar-powered city), `thought`, `airport`, `bakery`, `fabric`, and `modern` (comparison of old rotary phone with modern smartphone) are clear, child-appropriate cartoon images. `fist` is also clear as a simple illustrated hand shape.

### Do not accept automatically

`wildfire`, `skills`, `tips`, `ingredients`, `Khamsin`, `hide`, `pumped`, `pump`, `mud bricks`, `heat wave`, and `damaged` are photographic rather than cartoon. `increase`, `recipe`, `blood vessels`, and `blood` are text-heavy diagrams or infographics. They may be factually related but do not meet the established image-style requirement for the flashcards.

### Decision from visual review of all exact matches

The approved set will be restricted to pictures that are both semantically clear and visually compatible with the child-friendly cartoon library. Photographs, diagrams, infographics, images carrying large generated text, and the checkerboard-background waiter will remain unlinked even when their file name contains a vocabulary term.

## Semantic sheets 1–2

### Suitable or promising cartoon matches

`agreed`, `alive` (sprouting plant), `awful / terrible`, `bakery`, `beat`, `bright/shiny`, `bring`, `clapped`, `come`, `control`, and `cooked` (the illustrated pot of soup) are visually understandable, close to the intended meaning, and broadly consistent with the child-friendly illustration style.

### Rejected or not preferred

`[be] able to`, `added`, `bake`, both photographic `blocked by` scenes, `cover`, `crunchy / crispy`, and `cut into` are photographs. `bent` and `changes` are explanatory diagrams. The photographic pan of chicken is a weaker duplicate for `cooked` and will not be selected while the illustrated soup is available. The jar proposed for `contain` is visually less direct and remains unaccepted until a clearer candidate is found.

### Duplicate handling

The pair of `come` images and the multiple `blocked by`/`cooked` proposals will be reduced to at most one approved image per vocabulary term after the remaining sheets are reviewed.

## Semantic sheets 3–4

### Suitable or promising cartoon matches

`electricity`, `farming` (prefer the cartoon farmer planting seeds), `flipped`, `floated`, `get sick`, `get tired`, `go for a walk`, `green areas`, `hang out with`, `hit` (baseball), and `hit` (ball striking a wall) are clear cartoon illustrations. `give advice` is understandable but contains a large descriptive heading; it remains optional rather than automatically approved.

### Rejected or not preferred

All three `damaged` proposals, `drought`, the wheat-field `farming` image, `flowed`, `followed by`, and `hide` are photographs. The bell image proposed for `hit` is diagrammatic, while the baseball and ball-against-wall cartoons directly illustrate the intended action. The two `hit` candidates will be narrowed to one image after checking the lesson context.

### Style safeguard

Where a cartoon and photograph express the same idea, only the cartoon candidate will be retained. The review continues to exclude factual photographs even when they are high-confidence filename matches.

## Semantic sheets 5–6

### Suitable or promising cartoon matches

`home to` (bear family scene), `jewelry`, `joy`, `keep animals`, `leap` (choose a cartoon child or dolphin, not the frog photograph), `loaf / loaves`, `lose weight`, `made of`, `manage`, `mineral resources`, `power`, `powered`, and `quiet / calm` are clear illustrated candidates. `real-life heroes`, `realized`, and `received` are semantically clear but photographic and remain unaccepted under the style requirement.

### Rejected or not preferred

`irrigation` and `organ` are text-heavy diagrams. `prevent` and the three real-life/photo candidates are photographs. The birdhouse `home to` and frog `leap` are photographs and are weaker than their respective cartoon alternatives. The remaining choices will preserve one illustration per term and avoid using any image merely because its filename is related.

## Semantic sheets 7–8

### Suitable or promising cartoon matches

`rides`, `sea food`, `search for`, `search`, `shine`, `shiny`, `shopped`, `show respect`, `spread`, and `think hard` are clear cartoon candidates. `taking care of` through the illustrated watering scene is plausible but will be accepted only if no clearer caring-for-animal/plant image exists; its large “irrigation” heading is a distraction. `tiny` through the illustrated ant is also plausible, but the extra motivational text makes it a fallback rather than a preferred card image.

### Rejected or not preferred

`relaxing`, `replaced`, `sandstorm`, `shade`, `stay in balance`, the photographed `stethoscope`, photographed `taking care of`, both `then and now` candidates, `thin slices`, and the ladybug `tiny` candidate are photographs or text-heavy comparison graphics. The photo-based candidates will not enter the cartoon flashcard library.

## Semantic sheet 9

### Suitable or promising cartoon matches

`weather conditions`, `Well done! Excellent`, `whispered`, and `winds with sand` are clear child-oriented illustrations. For `winds with sand`, prefer the no-text swirling-sand illustration; the alternative labelled “Khamsin” is a useful backup when the lesson specifically uses that term.

### Rejected or not preferred

`treated`, `warnings`, and `work day and night` are photographs. They will remain outside the cartoon image map despite the clear relationship suggested by their filenames.

## Visual review complete

All 45 exact filename candidates and all 105 high-confidence semantic filename candidates have now been reviewed visually. The next step is to merge only the approved cartoon matches, enforce one image per Grade 5 term, and regenerate the missing-images report.
